// Minimal module declaration — react-simple-maps v3 ships no bundled types.
// Declaring the module keeps the build type-checking happy without pulling in
// a separately-versioned @types package.
declare module "react-simple-maps";
