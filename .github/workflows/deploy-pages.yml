name: Deploy to GitHub Pages

# Builds the static site and publishes it to GitHub Pages on every push to the
# feature branch. In the repo: Settings → Pages → Source → "GitHub Actions".
on:
  push:
    branches:
      - claude/privacy-ai-law-monitor-lybwto
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Build static export
        env:
          # Repo is served at /<repo-name> on a GitHub Pages project site.
          NEXT_PUBLIC_BASE_PATH: /monitor-law
        run: npm run build
      - name: Disable Jekyll processing
        run: touch out/.nojekyll
      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
